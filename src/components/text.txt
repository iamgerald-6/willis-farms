-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.users (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  role text NOT NULL DEFAULT '"employee"'::text CHECK (role = ANY (ARRAY['super_admin'::text, 'admin'::text, 'manager'::text, 'employee'::text])),
  email character varying NOT NULL,
  phone character varying NOT NULL,
  user_id character varying NOT NULL UNIQUE,
  password text,
  first_name text NOT NULL,
  last_name text NOT NULL,
  email_confirm text,
  email_verified text,
  company_id character varying NOT NULL DEFAULT ''::character varying UNIQUE,
  job_position character varying,
  grade_level text CHECK (grade_level = ANY (ARRAY['L1'::text, 'L2'::text, 'L3'::text, 'L4'::text, 'L5'::text, 'L6'::text, 'L7'::text])),
  final_score numeric,
  final_score_year integer,
  promotion_eligible boolean NOT NULL DEFAULT false,
  access_tier text NOT NULL DEFAULT 'standard'::text CHECK (access_tier = ANY (ARRAY['standard'::text, 'delegated'::text])),
  page_permissions jsonb NOT NULL DEFAULT '[]'::jsonb,
  access_updated_at timestamp with time zone,
  access_updated_by uuid,
  tm_can_view_all_tasks boolean NOT NULL DEFAULT false,
  is_disabled boolean NOT NULL DEFAULT false,
  CONSTRAINT users_pkey PRIMARY KEY (user_id)
);
CREATE TABLE public.content (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  title text NOT NULL,
  category character varying,
  sub_category character varying,
  description text,
  created_by text,
  video_duration_minutes text,
  document_read_minutes text,
  cover_image_url text,
  document_url text,
  video_url text,
  CONSTRAINT content_pkey PRIMARY KEY (id)
);
CREATE TABLE public.leave_requests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id character varying NOT NULL,
  leave_type text NOT NULL CHECK (leave_type = ANY (ARRAY['Annual'::text, 'Sick'::text, 'Emergency'::text, 'Maternity/Paternity'::text, 'Unpaid'::text, 'Other'::text])),
  reason text,
  start_date date NOT NULL,
  end_date date NOT NULL,
  total_days integer NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'approved'::text, 'rejected'::text])),
  admin_note text,
  reviewed_by character varying,
  reviewed_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT leave_requests_pkey PRIMARY KEY (id),
  CONSTRAINT leave_requests_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(user_id),
  CONSTRAINT leave_requests_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES public.users(user_id)
);
CREATE TABLE public.appraisals (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  company_id character varying NOT NULL,
  employee_name text NOT NULL,
  job_title text NOT NULL,
  current_grade text NOT NULL,
  section_authorisations_held text,
  immediate_supervisor text NOT NULL,
  cycle text NOT NULL,
  grade_band text NOT NULL,
  review_quarter text,
  review_year integer NOT NULL,
  reviewing_manager text,
  period_covered text,
  promotion_readiness text NOT NULL CHECK (promotion_readiness = ANY (ARRAY['not_yet_ready'::text, 'developing'::text, 'nearly_ready'::text, 'ready_for_assessment'::text, 'ready_for_expanded_responsibility'::text])),
  strengths_observed text,
  improvement_areas text,
  agreed_actions text,
  employee_comments text,
  most_significant_achievement text,
  development_plan_next_year text,
  promotion_readiness_assessment text,
  compensation_review_input text,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'submitted'::text, 'open'::text, 'final_reviewed'::text, 'locked'::text, 'reopened'::text, 'completed'::text])),
  employee_ratings jsonb,
  supervisor_ratings jsonb,
  employee_weighted_score numeric,
  supervisor_weighted_score numeric,
  submitted_by text DEFAULT 'none'::text,
  employee_company_id text,
  supervisor_company_id text,
  final_review_date date,
  final_review_notes text,
  employee_email text,
  supervisor_email text,
  deadline_at timestamp with time zone,
  employee_submitted_at timestamp with time zone,
  supervisor_submitted_at timestamp with time zone,
  locked_at timestamp with time zone,
  locked_reason text CHECK (locked_reason = ANY (ARRAY['employee_incomplete'::text, 'supervisor_incomplete'::text, 'reopen_incomplete'::text])),
  final_quarter_score numeric,
  supervisor_id uuid,
  employee_user_id uuid,
  reopened_deadline_at timestamp with time zone,
  appeal_exhausted boolean NOT NULL DEFAULT false,
  employee_penalty_points integer NOT NULL DEFAULT 0,
  CONSTRAINT appraisals_pkey PRIMARY KEY (id),
  CONSTRAINT appraisals_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.users(company_id)
);
CREATE TABLE public.off_days (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id character varying NOT NULL,
  day_of_week integer NOT NULL CHECK (day_of_week >= 0 AND day_of_week <= 6),
  created_at timestamp with time zone DEFAULT now(),
  effective_from date NOT NULL DEFAULT CURRENT_DATE,
  CONSTRAINT off_days_pkey PRIMARY KEY (id),
  CONSTRAINT off_days_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(user_id)
);
CREATE TABLE public.skill_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id character varying NOT NULL,
  supervisor_id character varying NOT NULL,
  log_type text NOT NULL,
  review_period text NOT NULL,
  section text,
  tier_auth text,
  strengths_observed text,
  development_gaps text,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'submitted'::text, 'signed_off'::text])),
  overall_rating numeric,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  signed_off_by character varying,
  signed_off_at timestamp with time zone,
  CONSTRAINT skill_logs_pkey PRIMARY KEY (id),
  CONSTRAINT skill_logs_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.users(user_id),
  CONSTRAINT skill_logs_supervisor_id_fkey FOREIGN KEY (supervisor_id) REFERENCES public.users(user_id),
  CONSTRAINT skill_logs_signed_off_by_fkey FOREIGN KEY (signed_off_by) REFERENCES public.users(user_id)
);
CREATE TABLE public.skill_log_competencies (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  skill_log_id uuid NOT NULL,
  skill text NOT NULL,
  observed text CHECK (observed = ANY (ARRAY['yes'::text, 'no'::text])),
  performed_under_supervision text CHECK (performed_under_supervision = ANY (ARRAY['yes'::text, 'no'::text])),
  performed_consistently text CHECK (performed_consistently = ANY (ARRAY['yes'::text, 'no'::text])),
  rating integer CHECK (rating >= 1 AND rating <= 5),
  comments text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT skill_log_competencies_pkey PRIMARY KEY (id),
  CONSTRAINT skill_log_competencies_skill_log_id_fkey FOREIGN KEY (skill_log_id) REFERENCES public.skill_logs(id)
);
CREATE TABLE public.promotions (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  appraisal_id integer UNIQUE,
  employee_company_id text NOT NULL,
  employee_name text NOT NULL,
  current_grade text NOT NULL,
  current_job_title text,
  proposed_grade text NOT NULL,
  proposed_job_title text NOT NULL,
  immediate_supervisor text,
  reviewing_manager text NOT NULL,
  triggering_review text,
  tier_authorisation text,
  section_unit text,
  eligibility_checklist jsonb NOT NULL DEFAULT '{}'::jsonb,
  assessment_ratings jsonb NOT NULL DEFAULT '{}'::jsonb,
  final_decision text NOT NULL CHECK (final_decision = ANY (ARRAY['promote'::text, 'promote_with_conditions'::text, 'defer_pending_skills'::text, 'retain_with_improvement'::text, 'not_ready'::text])),
  decision_comments text,
  conditions text,
  submitted_by_user_id character varying,
  submitted_by_grade text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  promotion_step text,
  time_in_current_role text,
  business_need_confirmed boolean,
  form_data jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT promotions_pkey PRIMARY KEY (id),
  CONSTRAINT promotions_appraisal_id_fkey FOREIGN KEY (appraisal_id) REFERENCES public.appraisals(id),
  CONSTRAINT promotions_submitted_by_user_id_fkey FOREIGN KEY (submitted_by_user_id) REFERENCES public.users(user_id)
);
CREATE TABLE public.manuals (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  title text NOT NULL,
  category text NOT NULL CHECK (category = ANY (ARRAY['HR'::text, 'Biosecurity'::text, 'Finance Policies'::text, 'Breeding Operations'::text])),
  description text,
  created_by uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT manuals_pkey PRIMARY KEY (id),
  CONSTRAINT manuals_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.manual_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  manual_id uuid NOT NULL,
  version_label text NOT NULL,
  cloudinary_public_id text NOT NULL,
  cloudinary_url text NOT NULL,
  file_name text NOT NULL,
  file_size_bytes integer,
  version_notes text,
  uploaded_by uuid NOT NULL,
  uploaded_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT manual_versions_pkey PRIMARY KEY (id),
  CONSTRAINT manual_versions_manual_id_fkey FOREIGN KEY (manual_id) REFERENCES public.manuals(id),
  CONSTRAINT manual_versions_uploaded_by_fkey FOREIGN KEY (uploaded_by) REFERENCES auth.users(id)
);
CREATE TABLE public.job_applications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  reference_number text NOT NULL UNIQUE,
  full_name text NOT NULL,
  email text NOT NULL,
  phone text NOT NULL,
  location text,
  role_slug text NOT NULL,
  role_title text NOT NULL,
  cover_note text,
  cv_url text,
  cv_public_id text,
  status text NOT NULL DEFAULT 'applied'::text CHECK (status = ANY (ARRAY['applied'::text, 'under_review'::text, 'shortlisted'::text, 'interview'::text, 'offer'::text, 'rejected'::text])),
  hr_notes text,
  interview_form_data jsonb NOT NULL DEFAULT '{}'::jsonb,
  interview_submitted_at timestamp with time zone,
  interview_submitted_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT job_applications_pkey PRIMARY KEY (id)
);
CREATE TABLE public.supervisor_penalties (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  supervisor_id uuid NOT NULL,
  appraisal_id bigint NOT NULL,
  review_quarter text NOT NULL,
  review_year integer NOT NULL,
  points_deducted integer NOT NULL DEFAULT 10,
  waived boolean NOT NULL DEFAULT false,
  justification_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT supervisor_penalties_pkey PRIMARY KEY (id),
  CONSTRAINT supervisor_penalties_appraisal_id_fkey FOREIGN KEY (appraisal_id) REFERENCES public.appraisals(id)
);
CREATE TABLE public.appraisal_justifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  appraisal_id bigint NOT NULL,
  supervisor_id uuid NOT NULL,
  reason_text text NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'approved'::text, 'rejected'::text])),
  reviewed_by uuid,
  reviewed_by_name text,
  review_notes text,
  reviewed_at timestamp with time zone,
  points_waived boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT appraisal_justifications_pkey PRIMARY KEY (id),
  CONSTRAINT appraisal_justifications_appraisal_id_fkey FOREIGN KEY (appraisal_id) REFERENCES public.appraisals(id)
);
CREATE TABLE public.tm_projects (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  description text,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'archived'::text])),
  created_by character varying NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tm_projects_pkey PRIMARY KEY (id),
  CONSTRAINT tm_projects_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(user_id)
);
CREATE TABLE public.tm_tasks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  project_id uuid NOT NULL,
  title text NOT NULL,
  description text,
  owner_id character varying,
  due_date date,
  is_recurring boolean NOT NULL DEFAULT false,
  task_type text NOT NULL DEFAULT 'general'::text CHECK (task_type = ANY (ARRAY['general'::text, 'obligation'::text, 'monitoring'::text])),
  frequency text,
  indicator text,
  method_provider text,
  lifecycle_status text NOT NULL DEFAULT 'active'::text CHECK (lifecycle_status = ANY (ARRAY['active'::text, 'completed'::text, 'archived'::text, 'deleted'::text])),
  completed_at timestamp with time zone,
  progress_percent integer NOT NULL DEFAULT 0 CHECK (progress_percent >= 0 AND progress_percent <= 100),
  source text NOT NULL DEFAULT 'manual'::text CHECK (source = ANY (ARRAY['manual'::text, 'ai_extracted'::text])),
  source_document_url text,
  source_document_name text,
  created_by character varying NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tm_tasks_pkey PRIMARY KEY (id),
  CONSTRAINT tm_tasks_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.tm_projects(id),
  CONSTRAINT tm_tasks_owner_id_fkey FOREIGN KEY (owner_id) REFERENCES public.users(user_id),
  CONSTRAINT tm_tasks_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(user_id)
);
CREATE TABLE public.tm_task_audit_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  task_id uuid NOT NULL,
  project_id uuid NOT NULL,
  action text NOT NULL CHECK (action = ANY (ARRAY['created'::text, 'edited'::text, 'archived'::text, 'deleted'::text, 'restored'::text, 'completed'::text])),
  changed_fields jsonb,
  previous_values jsonb,
  new_values jsonb,
  performed_by character varying NOT NULL,
  performed_by_name text NOT NULL,
  performed_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tm_task_audit_log_pkey PRIMARY KEY (id),
  CONSTRAINT tm_task_audit_log_task_id_fkey FOREIGN KEY (task_id) REFERENCES public.tm_tasks(id),
  CONSTRAINT tm_task_audit_log_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.tm_projects(id),
  CONSTRAINT tm_task_audit_log_performed_by_fkey FOREIGN KEY (performed_by) REFERENCES public.users(user_id)
);
CREATE TABLE public.tm_extraction_jobs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  project_id uuid NOT NULL,
  file_name text,
  file_url text,
  files jsonb NOT NULL DEFAULT '[]'::jsonb,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'completed'::text, 'failed'::text])),
  extracted_tasks jsonb,
  error_message text,
  created_by character varying NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tm_extraction_jobs_pkey PRIMARY KEY (id),
  CONSTRAINT tm_extraction_jobs_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(user_id),
  CONSTRAINT tm_extraction_jobs_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.tm_projects(id)
);
CREATE TABLE public.tm_monthly_reports (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  period_start date NOT NULL,
  period_end date NOT NULL,
  pdf_url text,
  sent_to jsonb NOT NULL DEFAULT '[]'::jsonb,
  stats_snapshot jsonb,
  generated_by character varying,
  generated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tm_monthly_reports_pkey PRIMARY KEY (id),
  CONSTRAINT tm_monthly_reports_generated_by_fkey FOREIGN KEY (generated_by) REFERENCES public.users(user_id)
);
CREATE TABLE public.tm_task_completions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  task_id uuid NOT NULL,
  project_id uuid NOT NULL,
  due_date date,
  completed_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_by character varying NOT NULL,
  completed_by_name text NOT NULL,
  CONSTRAINT tm_task_completions_pkey PRIMARY KEY (id),
  CONSTRAINT tm_task_completions_task_id_fkey FOREIGN KEY (task_id) REFERENCES public.tm_tasks(id),
  CONSTRAINT tm_task_completions_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.tm_projects(id),
  CONSTRAINT tm_task_completions_completed_by_fkey FOREIGN KEY (completed_by) REFERENCES public.users(user_id)
);
CREATE TABLE public.tm_report_schedule (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  enabled boolean NOT NULL DEFAULT false,
  day_of_month integer NOT NULL DEFAULT 1 CHECK (day_of_month >= 1 AND day_of_month <= 28),
  recipients jsonb NOT NULL DEFAULT '[]'::jsonb,
  last_sent_period text,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tm_report_schedule_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tm_reminder_settings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  enabled boolean NOT NULL DEFAULT true,
  days_before_due integer NOT NULL DEFAULT 14 CHECK (days_before_due >= 1 AND days_before_due <= 30),
  cc_recipients jsonb NOT NULL DEFAULT '[]'::jsonb,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tm_reminder_settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tm_reminder_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  task_id uuid NOT NULL,
  reminder_type text NOT NULL CHECK (reminder_type = ANY (ARRAY['due_soon'::text, 'overdue'::text])),
  sent_on date NOT NULL DEFAULT CURRENT_DATE,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tm_reminder_log_pkey PRIMARY KEY (id),
  CONSTRAINT tm_reminder_log_task_id_fkey FOREIGN KEY (task_id) REFERENCES public.tm_tasks(id)
);