/**
 * Analytics placeholders.
 * - Add GA4 via NEXT_PUBLIC_GA4_ID
 * - Add GTM via NEXT_PUBLIC_GTM_ID
 *
 * Note: These are placeholders and do not load anything by default.
 */
export function Analytics() {
  return null;
}
