import type { ModuleRecord } from "./types";
import { NAV_BUILTIN_MODULES } from "./modules/navModules";

/** Built-in modules shipped with the app (source: Git today, seed for DB later). */
export const BUILTIN_MODULES: ModuleRecord[] = NAV_BUILTIN_MODULES;
